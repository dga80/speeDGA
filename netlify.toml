[build]
  command = "if [ -d \"flutter\" ]; then flutter build web; else git clone https://github.com/flutter/flutter.git -b stable && ./flutter/bin/flutter build web; fi"
  publish = "build/web"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
